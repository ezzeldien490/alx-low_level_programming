#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	const char *massage = "\"Programming is like building a multilingual puzzle";

	puts(massage);

	return (0);
}
